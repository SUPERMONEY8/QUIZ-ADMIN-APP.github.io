[build]
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA Routing - All routes redirect to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

